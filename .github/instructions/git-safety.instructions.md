---
description: Git Safety
applyTo: "**"
---

## Git Rules

- **NEVER** run `git push`, `git commit`, or `git add` unless the user **explicitly** requests it in the current prompt
- A previous prompt requesting push does NOT carry over — each prompt is independent
- Reading `git status`, `git diff`, and `git log` is allowed at any time
